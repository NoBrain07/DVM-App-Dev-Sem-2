# Fest Events Explorer

This is a React Native App which shows Events from the backend and  displays them in the app

It is only built and tested for android devices and not built and run on ios devices